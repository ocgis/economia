<%= form_for(date_entered) do |f| %>
  <% if date_entered.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(date_entered.errors.count, "error") %> prohibited this date_entered from being saved:</h2>

      <ul>
      <% date_entered.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
