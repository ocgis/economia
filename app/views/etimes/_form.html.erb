<%= form_for(etime) do |f| %>
  <% if etime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(etime.errors.count, "error") %> prohibited this etime from being saved:</h2>

      <ul>
      <% etime.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
