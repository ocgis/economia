<p id="notice"><%= notice %></p>

<h1>Etransactions</h1>

<table>
  <thead>
    <tr>
      <th>Id </th>
      <th>Description</th>
      <th>Num</th>
      <th>Currency id </th>
      <th>Currency space</th>
      <th>Date entered date</th>
      <th>Date entered ns</th>
      <th>Date posted date</th>
      <th>Date posted ns</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @etransactions.each do |etransaction| %>
      <tr>
        <td><%= etransaction.id_ %></td>
        <td><%= etransaction.description %></td>
        <td><%= etransaction.num %></td>
        <td><%= etransaction.currency_id_ %></td>
        <td><%= etransaction.currency_space %></td>
        <td><%= etransaction.date_entered_date %></td>
        <td><%= etransaction.date_entered_ns %></td>
        <td><%= etransaction.date_posted_date %></td>
        <td><%= etransaction.date_posted_ns %></td>
        <td><%= link_to 'Show', etransaction %></td>
        <td><%= link_to 'Edit', edit_etransaction_path(etransaction) %></td>
        <td><%= link_to 'Destroy', etransaction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Etransaction', new_etransaction_path %>
