<%= form_for(reconcile_date) do |f| %>
  <% if reconcile_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reconcile_date.errors.count, "error") %> prohibited this reconcile_date from being saved:</h2>

      <ul>
      <% reconcile_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
